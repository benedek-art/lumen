# R3 — DxO PhotoLab 10 / 9.6 + FilmPack 8 (as of 2026-09-01)

Dossier for the W1 audit. Written for auditors who each read one lettered section; each row is
capability · how it works · source tag · what Lumen would need (grep-grounded in `Sources/`).

**Version snapshot, verified this session (all NEW relative to docs/03 §2 and docs/17 C):**

| Product | Current | Date | Evidence |
|---|---|---|---|
| PhotoLab **10** | 10.0 | **1 Sep 2026 (today)** | Depth Masks, Control Brush, elliptical Control Points, Nik-9 tone-zone grading wheel, "AI add-ons"; $249.99 new / $129.99 upgrade from 8 or 9 [search: https://petapixel.com/2026/09/01/dxo-releases-photolab-10-with-ai-add-ons-masking-and-color-grading-updates/ ; https://camerajabber.com/photography-news/dxo-photolab-10-price-specifications-and-availability-announced/ ; https://www.dpreview.com/news/dxo-photolab-10-announcement/] |
| PhotoLab 9 | 9.0 → 9.2 → **9.6** | 2 Sep 2025 → 17 Mar 2026 | 9.0: AI Masks, local denoise/demosaic/lens-sharpness, ProRAW+HEIC, stacks, Favorites; £219.99/£109.99. 9.2: better matting + sensitivity threshold. 9.6: DeepPRIME **XD3 for Bayer**, AI-mask diffusion (feather) slider, High-Fidelity DNG compression (≤4× smaller) [search: https://petapixel.com/2025/09/02/dxo-photolab-9-is-biggest-upgrade-to-the-raw-photo-editor-in-20-years/ ; https://lifeafterphotoshop.com/dxo-photolab-9-6-update-deepprime-xd3-compressed-dng-files-and-feathered-ai-masks/ ; https://photorumors.com/2026/03/17/dxo-photolab-version-9-6-released/] |
| FilmPack **8** | 8.x | 30 Sep 2025 | 153 renderings (+15), Time Warp mode, Time Machine +17 images, hi-res re-rendered effects, per-rendering unique grain, full Photoshop integration. **No FilmPack 9 found** as of today [search: https://petapixel.com/2025/09/30/dxo-filmpack-8-takes-your-favorite-photos-back-through-time/ ; https://www.photoreview.com.au/news/products/dxo-launches-filmpack-8-film-emulation-software/] |
| PureRAW | **6** exists (features folded into PL 9.6) | ≤ Mar 2026 | [search: https://fstoppers.com/post-production/dxo-adds-pureraw-6-features-photolab-9-900873] — feature list not verified |
| Nik Collection | **9** exists (source of PL10's grading wheel) | ≤ Sep 2026 | [search: DPReview PL10 announcement above] — feature list not verified |

Web reach: `WebSearch` worked throughout. `WebFetch` is **egress-blocked on www.dxo.com and
userguides.dxo.com** (tried once each); every DxO-authored fact below therefore comes via search
result summaries of those pages, tagged `[search: …]`, or from training, tagged `[knowledge]`.

---

## A · Tone & sliders

| Capability | How it works | Source | Lumen would need |
|---|---|---|---|
| **Smart Lighting** | Zone-adaptive tone (single-shot-HDR lineage). Two modes: **Uniform** and **Spot Weighted**. Intensity 0–100 with Slight / Medium / Strong buttons (≈25/50/75). Spot Weighted **auto-detects faces**; the count of detected areas is shown in the sub-palette; clicking the tool icon reveals the boxes on canvas; the user can **draw additional weighting boxes** ("preserved areas", as many as needed) with the framing tool. Clicking Spot Weighted defaults to Slight; reviewers advise avoiding Strong on raw. Dates from OpticsPro 11 (2016). | [docs/03 §2.3] [search: https://tutodxo.com/en/dxo-smart-lighting/ ; https://photographicfreedom.com/spot-weighted-dxo-smart-lighting] | `AutoTone.suggest` exists (`Sources/LumenApp/AppStateActions.swift:39`), but **no face detection** anywhere (0 hits for `VNDetectFace|faceRect|faceWeight`) and no user-drawn weight regions. |
| **Exposure compensation** | Slider ±4 EV with Auto sub-modes (centre-weighted average, spot, highlight-priority) | [knowledge] | Present: `Tone.exposure` (`Recipe.swift:394`). |
| **Selective Tone** | Highlights / Midtones / Shadows / Blacks, each −100…+100, default 0; documented as equivalent to placing points at the ends and middle of the tone curve. Also available inside Local Adjustments. | [search: https://tutodxo.com/en/selective-tone/] [knowledge for ranges] | Present as `Tone.highlights/shadows/whites/blacks` plus a 5-zone `Zones` model with movable `pivots` (`Recipe.swift:437–453`) — richer than DxO's four fixed bands. |
| **Contrast palette** | Contrast, **Microcontrast**, and (FilmPack-gated) **Fine contrast** with its own Highlights / Midtones / Shadows sub-sliders; Fine contrast is gentler than Microcontrast, protects flat/low-detail areas, and reviewers find it removes clipping better than Selective Tone's namesakes. | [search: https://www.dpreview.com/forums/threads/dxo-filmpack-contrast-tools.4683557/ ; https://lifeafterphotoshop.com/how-to-get-great-black-and-white-in-dxo-photolab-but-youll-need-filmpack-5/] | `Detail.texture/clarity/dehaze` (`Recipe.swift:783–785`) cover Microcontrast/ClearView; **no tone-range-limited local contrast** (Fine contrast Highlights/Midtones/Shadows). |
| **ClearView Plus** | One 0–100 slider. Analyses each region for lost definition, then expands local contrast to "restore true blacks, pure whites", raising micro-contrast, cutting haze and boosting vibrance in one move. DxO's claim: **no edge halos** unlike LR Dehaze/Texture/Clarity, holds up when pushed. Reviewer reality: heavy-handed, "grungy" colour at high values, gritty skin. Available locally (equalizer/Local Adjustments). | [docs/03 §2.3] [search: https://www.dxo.com/en/technology/clearview/ ; https://www.capturelandscapes.com/dxo-photolab/] | Lumen deliberately keeps Dehaze and Clarity separate (`Detail.dehaze`, `Detail.clarity`) — by design (docs/03). Auditor: check halo behaviour of Lumen's dehaze against DxO's no-halo claim. |
| **Tone Curve** | Master RGB, R, G, B and a **Luma** channel ("Luma control", PL8) that moves tones without shifting colour; drag points; per-channel. FilmPack 8 adds 10+ curve presets for inverting scanned negatives. | [search: https://amateurphotographer.com/review/dxo-photolab-8-review-the-best-raw-editor-money-can-buy/ ; https://www.dxo.com/en/dxo-filmpack/whats-new/] | Present: `CurveSet.point/r/g/b/luma` + `preserveLuminance` (`Recipe.swift:532–541`). |
| **Loupe Preview** | PL8 toolbar tool (replaced the old magnifier); a large floating live preview, zoom to **1600%**, showing DeepPRIME/demosaic in full quality plus Exposure, ClearView, Smart Lighting, curve. PL9: optional **full-screen DeepPRIME preview** on "top-spec" machines (reviewer wording). | [search: https://www.provideocoalition.com/dxo-photolab-8-deepprime-xd2s-hue-masks-and-a-loupe/ ; https://medium.com/the-technology-times/dxo-photolab-review-d80999d1a287] | `LoupeView.swift` exists; Lumen's differentiator (full-image cached AI preview + blend) is `Denoise.amount` "instant post-compute" (`Recipe.swift:1055`). |

## B · Colour & grading

| Capability | How it works | Source | Lumen would need |
|---|---|---|---|
| **Color Rendering** | Palette: **Category** (Generic renderings / Camera body / Film — needs FilmPack / **DCP profile**), **Rendering** (e.g. Neutral color, realistic; DxO camera profile; specific bodies), **Intensity** 0–100 (100 = standard), **Protect saturated colors** slider (reworked in PL6.3). DCP curve menu offers **DxO** (crisper, uses full Wide Gamut) vs **Adobe** (matches LR/ACR). **ICC input profiles dropped in PL7 (Sep 2023)** — DCP only. Switching to B&W resets Type/Rendering to Generic/Neutral. | [search: https://www.dpreview.com/forums/thread/4322239 ; https://forum.dxo.com/t/issues-with-dcp-icc-profiles/6725 ; https://www.provideocoalition.com/dxo-photolab-6-3-has-new-wide-gamut-and-soft-proofing-options/] | 0 hits for `DCP|cameraProfile` in `Sources/`. Lumen has `RenderParams.preset` Neutral/Soft/Punchy/Film Base/Linear with contrast/skew/huePreservation (`RecipeLook.swift:125–132`) — a display-rendering choice, not a camera-colour choice. Auditor: DCP loading is **absent**. |
| **DxO Wide Gamut WCS** | Working colour space since PL6 (2022), "significantly larger than Adobe RGB"; primaries never published. Per-image choice of Classic (legacy) vs Wide Gamut. Soft Proofing palette (6.3): output profile, **Preserve color details** slider (protects saturated detail on gamut-down), **monitor gamut warning** overlay. | [search: https://www.dxo.com/news/white-paper-wide-gamut/ ; https://chriswright.photography/dxo-wide-gamut-color-space/] | Lumen: linear Rec.2020 working space (docs/14), `SoftProofTransform.swift` present. Check for a monitor-gamut warning overlay (`ViewerOverlays.swift`). |
| **HSL ColorWheel** | 8 channel swatches (R, O, Y, G, C, B, P, M) + all; eyedropper picks the channel; the selected range is an **arc on the hue ring with four handles** — two inner (range) and two outer (feather); rotate the arc to shift hue; sliders Hue, Saturation, Luminance (−100…+100) and **Uniformity** (left = spread hues apart, right = converge toward the mean; skin-blemish use case). Before/after swatch pair. Also in Local Adjustments (PL7). | [docs/03 §2.4] [search: https://chriswright.photography/hsl-tool-in-photolab-4/ ; https://lifeafterphotoshop.com/the-new-hsl-local-adjustments-in-dxo-photolab-7-explained/] | Present: `Mixer.bands[8]` with `core`/`feather` arrays, `uniformity` 0…100 (`Recipe.swift:614–663`), `PointColor.variance` (−100…+100). Verify the on-canvas arc/handle grammar exists in `ColorPanel.swift`. |
| **Colour grading wheel (PL10, from Nik 9)** | One wheel carrying **shadows, midtones, highlights and global** colour — tone-zone grading, explicitly distinct from the hue-targeted ColorWheel. | [search: https://www.dpreview.com/news/dxo-photolab-10-announcement/ ; https://fstoppers.com/apps/dxo-releases-photolab-10-depth-masks-advanced-color-grading-and-new-ai-tools-904420] | Present and deeper: `GradingWheels.global/shadows/mid/high` + `blending`, `balance`, `pivots` (`RecipeLook.swift:229–236`). |
| Vibrancy / Saturation; Temperature / Tint with presets | Standard; Vibrancy protects skin/skies. | [knowledge] | Present: `ColorAdjust.vibrance/saturation/density/protectSkin` (`Recipe.swift:296–299`). |
| **B&W** | Via Color Rendering (B&W films need FilmPack) + FilmPack's Channel mixer, colour Filter, Toning. | [knowledge] | `Look.bw` (`RecipeLook.swift:13`) present. |

## C · Film & grain (FilmPack 8)

| Capability | How it works | Source | Lumen would need |
|---|---|---|---|
| **Stock library** | **153 film renderings** (FP8 added 15 incl. CineStill 800T, Harman Phoenix 200, Ilford Lady Grey 400, Kodak T-MAX P3200) across colour negative, colour positive/slide, B&W, cross-processed, and instant categories; FP8 also added **4 digital-camera renderings** (Fujifilm/Sony JPEG looks). 300+ presets in total. Per-category counts: not found. | [search: https://www.photoreview.com.au/news/products/dxo-launches-filmpack-8-film-emulation-software/ ; https://www.digitalcameraworld.com/cameras/film-cameras/dxo-filmpack-8-launches-with-full-adobe-photoshop-integration-and-two-powerful-new-sliders] category list [knowledge] | Lumen: **6 deeply-modelled stocks** by design (`FilmLab.swift:126`: portra400, gold200, ektar100, triX400, + transparencies). Breadth is a deliberate non-goal (D18). |
| **How a stock is modelled** | DxO shoots **calibrated targets + real subjects on the actual film**, has it developed by named labs, high-res digitises it, and extracts (a) a **colorimetric profile** — hue/saturation response vs exposure, blacks/shadows/mids/highlights, contrast — and (b) **grain matrices** taken "directly from the real silver-halide crystals", analysed separately for shadows, midtones and highlights. Applied as: colour rendering (3D colour transform) + tone response + grain. **Intensity** slider blends the rendering. | [search: https://www.dxo.com/dxo-filmpack/science-of-film/ ; https://www.dxo.com/en/dxo-filmpack/technology/emulation/] | Lumen models the physics instead of sampling: `FilmCharacteristic` (per-channel γ, dMin, dMax, logOffset), `FilmCrossover` (shadow/highlight/push tints, DIR `interlayer`, `coupler`), negative→print chain, `systemGamma` (`FilmLab.swift:43–213`). Auditor: Lumen's are "honestly named" own interpretations, not scans. |
| **Grain** | Film grain palette: **Film** dropdown (pick any stock's grain independently of the rendering), **Intensity**, **Size** (scaled to output size; a print-size notion exists in older FP — verify), **Randomize**. FP8: grain pattern is **unique per rendering** (no tiled repetition). Luminance-dependent (shadow/mid/highlight matrices). | [search: https://userguides.dxo.com/filmpack/en/customize-tab/ (via summary); https://www.dxo.com/en/dxo-filmpack/whats-new/] | Present and deeper: `FilmGrain.size/amount`, `FilmLab.printSize` anchor, per-channel plate size (0.8/1.0/2.0 RGB) so grain is chromatically structured (`FilmLab.swift:167–176`; `RecipeLook.swift:457–459`); `CreativeGrain.amount/size/roughness` (`Recipe.swift:201–204`). Lumen lacks DxO's "grain from stock X on rendering Y" decoupling only if the UI hides it — check `LookPanel.swift`. |
| **Halation** | **No halation control found in FilmPack 8** (searches for the term return nothing DxO-authored; CineStill 800T's look is baked into its rendering). | [search: negative result across FP8 reviews] | Lumen has a physical halation stage: per-channel strength (0.05, 0.015, 0) red-dominant, `halation` amount + redness (`FilmLab.swift:158–164`, `RecipeLook.swift:420`). **Lumen leads here.** |
| **Light leaks** | Light leak tool: **Defect** dropdown (presets; default "No defect"), **Intensity** (default 50). Re-rendered at high resolution in FP8. | [search: userguides summary above] | **Absent**: 0 hits for `lightLeak`. |
| **Vignette (creative)** | FilmPack Creative vignetting: intensity, midpoint, roundness, transition/feather; PhotoLab's own Vignetting palette is corrective (module-driven) with a manual mode. | [knowledge] | Present: `Look.vignette` + `vignetteFeather` (`RecipeLook.swift:19–27`) — no midpoint/roundness. |
| **Textures / Frames / Blur / Filter / Toning** | Textures (scratches, stains, grains, wrinkled paper) with Randomize; Frames (film borders, paper edges); Blur (soft focus, vignette blur); colour Filter for B&W; Toning (split); Channel mixer; **Luminosity masks** (FP8) to limit any of the above tonally. | [search: https://silentpeakphoto.com/photo-editing-apps/photo-editing-app-reviews/dxo-filmpack-review/] | Absent: textures, frames, blur. Toning is covered by grading wheels; luminosity gating by `MaskKind.luminosity`. |
| **Time Machine / Time Warp** | Time Machine: a gallery of historical eras (daguerreotype → 35 mm colour), each with sample images and their rendering; FP8 adds 17 images. **Time Warp** (FP8, new): two sliders — **Time Travel** scrubs ~200 years of process-authentic looks (grain, colour shifts, degradation); the second slider's name not verified. | [search: https://fstoppers.com/reviews/film-pack-8-out-full-photoshop-integration-and-clever-time-warp-mode-712767] | Not applicable to Lumen's scope. |
| **Hosting** | Standalone, PhotoLab plug-in (renderings appear inside Color Rendering), Lightroom plug-in, and (FP8) full Photoshop plug-in. | [search: PetaPixel FP8] | N/A — Lumen's Film Lab is a pipeline stage. |

## D · Looks / presets & effects

| Capability | How it works | Source | Lumen would need |
|---|---|---|---|
| Presets | "DxO Standard" default preset; preset editor; **partial presets** (only chosen palettes); PL9: **local-adjustment presets keyed on Subject Types** (copy/paste a sky edit between images and the AI mask re-targets). FilmPack ships 300+. | [search: https://www.dxo.com/en/dxo-photolab/features/ (summary)] | `LookSubset.swift`, `RecipeLook.swift` present; check whether a mask preset can carry an AI-kind component that re-resolves per image (`MaskKind.aiSky` etc. exist, `RecipeMasks.swift:250–255`). |
| LUTs | PL8+ supports .cube LUTs in the Color Rendering "LUT" category (intensity blend). | [knowledge] | Present: `LUTReference` with display/log tap + amount (`RecipeLook.swift:197–202`). |
| Effects | See §C: light leaks, textures, frames, blur — all FilmPack. | | Absent as above. |

## E · Denoise & sharpening

| Capability | How it works | Source | Lumen would need |
|---|---|---|---|
| **Denoising modes** | Palette offers **HQ**, **PRIME**, **DeepPRIME**, **DeepPRIME XD2s** (Bayer, PL8), **DeepPRIME XD3** (X-Trans first — PureRAW 5 / PL9; **Bayer since PL 9.6, Mar 2026**), and **DeepPRIME 3** (PureRAW 5, 2025; faster tier). DeepPRIME-class = **joint demosaic + denoise CNN on the raw mosaic**; runs on GPU/Neural Engine on Apple silicon. | [docs/03 §2.1] [search: https://luminous-landscape.com/dxo-photolab-9-6-deepprime-xd3-support-for-every-camera-and-storage-solutions/ ; https://www.dxo.com/en/news/introducing-dxo-deepprime-3/] | `Denoise.mode/amount/model/classic` (`Recipe.swift:1054–1057`); Tier-1 classical = profiled VST + à-trous shrinkage (`DenoiseEngine.swift`); raw-domain via `RawSource` is v2 (docs/07). |
| **Controls** | HQ / PRIME: **Luminance** (default 40), **Chrominance** (100), **Low frequency** (75), **Dead pixels** (24), **Maze** (30). DeepPRIME: **Luminance** + **Dead pixels** only. XD2s / XD3: Luminance, Dead pixels, **Force details** (checkbox/slider; reviewers back it off slightly). **X-Trans: no Dead pixels and no Maze** options. Chrominance is automatic in all DeepPRIME tiers. | [search: https://forum.dxo.com/t/prime-noise-its-settings-and-working-it-different-iso-values/4455 ; https://amateurphotographer.com/review/dxo-photolab-8-review-the-best-raw-editor-money-can-buy/ ; https://support.dxo.com/hc/en-us/articles/6940871314845] | Lumen: `ClassicNR` luma/chroma fractions; hot-pixel handling exists (5 files match `deadPixel|hotPixel`). **No Maze / Low-frequency / Force-details equivalents.** |
| **When it runs** | DeepPRIME is **not live in the main viewer**. Preview only in the Loupe (PL8+, ≤1600%) or the PL9 full-screen option on strong hardware; the full image renders at export. PL9 adds **DeepPRIME, demosaic and Lens Sharpness inside local masks**. | [docs/03 §2.1] [search: ProVideo Coalition PL8; Medium PL9 review; https://blog.thomasfitzgeraldphotography.com/blog/2025/9/dxo-releases-photolab-9-with-ai-masking-and-expanded-local-adjustments] | Lumen's stated advantage: full-image cached preview + instant `amount` blend. `LocalAdjust` has texture/clarity but **no per-mask denoise** field (`RecipeMasks.swift:614–643`). |
| **Speed (Apple silicon, forum-measured)** | XD2s on M1 Max Studio (32-core GPU, PL 8.1): **3.4 MP/s** → ≈13 s per 45 MP, 18 s per 61 MP. DeepPRIME 3 ≈ **3× faster than XD2s**, ≈4 s/image on the same machine. Another report: 24 MP at 11.6 s/image (XD2s, lower-tier Mac). XD3 Bayer timings: not found. | [search: https://www.dpreview.com/forums/threads/dxo-photolab-performance-benchmarks.4782804/ ; https://forum.dxo.com/t/deepprime-performance-on-m4-vs-m1-2-3/41655 ; https://www.dpreview.com/forums/threads/performance-tests-on-photolab-9.4820836/] | Reference for docs/07 budget: LR ANE ≈5 s/45 MP; DxO XD-class 10–20 s. |
| **Lens Sharpness** | Needs an Optics Module. **Global** default **+1.00** since PL 6.3 (double-click resets to 1; 0 still sharpens — it is the lab optimum), range −3…+3 [docs/03, still unconfirmed]; **Details** 50 and **Bokeh** 50 (0–100). Spatially varying deconvolution driven by the module's measured blur field (centre-to-corner). Users report Details/Bokeh rarely show a visible effect. | [search: https://forum.dxo.com/t/new-in-dxo-photolab-6-3-lens-sharpness-default-is-now-1/31413 ; https://chriswright.photography/ultimate-guide-to-image-sharpening-with-dxo/ ; https://www.dxo.com/en/technology/lens-sharpness/] | `CaptureSharpen.auto/radius/amount` (`Recipe.swift:32–41`) — auto-radius, **no measured per-lens blur field**, no corner map (grep `distortion|LCP|opcode` = 2 files, none a blur map). |
| **Unsharp Mask** (fallback when no module) | Intensity, Radius, Threshold, Edge offset; recommended start 100 / 0.5 / 4 (threshold 4–10). | [search: Chris Wright guide above] | `ManualSharpen.amount 0…150 / radius 0.5…3 / detail / masking / haloSuppression` (`Recipe.swift:86–91`) — richer; no edge-offset. |
| **Chromatic aberration** | Palette: lateral CA (auto with module; manual intensity), **Purple fringing** intensity + size; longitudinal CA corrected via module + focus distance. | [search: https://www.dxo.com/technology/how-dxo-corrects-lens-flaws/] | `LensCorrections.removeCA` + `Defringe?` (`Recipe.swift:1325–1328`). |

## F · Masks

| Capability | How it works | Source | Lumen would need |
|---|---|---|---|
| **Control Point (U Point)** | Click to drop a point with a radius circle; the mask is built from pixels inside the circle whose **luminosity, contrast and colour** resemble the sampled centre (Nils Kokemohr's Nik algorithm, re-implemented on raw data). **Chroma** and **Luma** selectivity sliders 0–100 (≈50 default) widen/narrow the similarity; **negative points** subtract; per-point **opacity**. **PL10: points can be switched from circular to elliptical.** | [docs/03 §2.5] [search: https://www.dxo.com/en/technology/u-point/ ; https://www.ppa.com/ppmag/articles/review-dxo-photolab-adds-control-points ; Fstoppers PL10] | Present: `MaskKind.similarity` with chroma/luma selectivity (docs/08) (`RecipeMasks.swift:248`). **Elliptical similarity region absent** — check `MaskHandles.swift` for an ellipse handle set. |
| **Control Line** | A graduated filter whose gradient is gated by similarity to a **detachable eyedropper** sample ("a grad that only hits sky-coloured pixels"), same Chroma/Luma sliders. | [docs/03 §2.5] | Present: `MaskKind.similarityLine` (`RecipeMasks.swift:249`). |
| **Control Brush (PL10)** | Brush stroke × U Point similarity: paints only pixels resembling the sampled colour/luminosity under the stroke. | [search: https://www.fujirumors.com/dxo-photolab-10-launched-with-depth-masks-advanced-color-grading-and-expanded-ai-powered-tools/] | Composable: `MaskOp.intersect` of `brush` and `similarity` (`RecipeMasks.swift:161–162`) — verify the UI offers it as one gesture. |
| **Graduated filter / Brush / Auto mask** | Standard grad with rotation and feather; brush with size, feather, flow, opacity; PL≤8 "Auto mask" edge-aware brush. | [knowledge] | `linear`, `brush` (+ `BrushStabilizer.swift`), `polygon` present. |
| **Luminosity mask** | Introduced **PL7** (Sep 2023): range sliders on a luminance ramp with feather; combinable with other masks. | [docs/03 §2.5 says PL7-era; docs/17 item 12 flags it] [knowledge] | Present: `lumaRange`, `luminosity` (Lights/Darks/Midtones series) (`RecipeMasks.swift:218–246`). |
| **Hue mask** | **PL8** (Oct 2024): select by hue range on a ring, with feather. | [search: ProVideo Coalition PL8] | Present: `colorRange`. |
| **AI Masks (PL9)** | Three entry paths: **hover + click** (object highlights under the cursor), **draw a box** around an object, or pick a **Subject Type**: sky, subject, background, people, animals, flowers, vehicles, hair, faces, clothes. Local model, no cloud, no credits. 9.2: improved matting + a sensitivity threshold; **9.6: diffusion (feather) slider**. Masks can be **subtracted, inverted, duplicated, combined** with control points/lines/grads/brush. | [search: https://lifeafterphotoshop.com/dxo-photolab-9-ai-masking-tools-explained-with-examples/ ; https://fstoppers.com/reviews/dxo-photolab-9-out-amazing-ai-masks-and-more-709773 ; Life after Photoshop 9.6] | Present kinds: `aiSubject/aiSky/aiBackground/aiObject/aiPerson/aiLandscape` with `MatteProvider.vision/.model` (`RecipeMasks.swift:250–342`, `VisionMattes.swift`). **Hover-to-highlight and box-prompt entry** — check `MaskCanvas.swift`; no `hair/clothes/animals/vehicles/flowers` classes named in code. |
| **Depth Mask (PL10)** | AI estimates a **depth map from any raw** (no embedded depth needed); mask by distance band — foreground / midground / background — with gradual falloff; demo use: darken with distance, add background blur to fake shallow DOF. Lives in the Local Adjustments palette. | [search: DPReview PL10; Luminous Landscape https://luminous-landscape.com/dxo-photolab-10-worth-the-upgrade/] | `MaskKind.depthRange` exists with `MatteProvider.model` (`RecipeMasks.swift:256, 342`) — verify a depth model is actually wired (grep for a depth model name returned only UI comments). |
| **Mask manager & panel** | PL7 replaced the on-canvas **Equalizer** (mini-sliders floating beside the point) with a docked Local Adjustments palette + a **masks list**: visibility toggle, per-mask **opacity**, invert, duplicate, subtract, show-mask overlay; Chroma/Luma sensitivity live in the list. The equalizer's 10 sliders survive as the palette: exposure, contrast, microcontrast, ClearView, vibrancy, saturation, temperature, tint, sharpness, blur — plus **HSL (PL7)**, **tone curve, and PL9's DeepPRIME/demosaic/Lens Sharpness**. | [docs/03 §2.5] [search: https://rangefinderonline.com/gear/software/dxo-photo-lab-review/ ; Thomas Fitzgerald PL9] | `MaskPanel.swift` + `MaskFloatingPanel.swift` (both registers — the docs/03 lesson). `LocalAdjust` has exposure…blacks, temp/tint/kelvin, hue/sat/vibrance, texture/clarity (`RecipeMasks.swift:614–643`); `Mask.amount 0…200`, `invert`, `blend` (normal/luminosity/colour), groups. **Missing vs DxO: local blur, local sharpen, local denoise, local ClearView-class dehaze.** |

## G · UI / UX

| Capability | How it works | Source | Lumen would need |
|---|---|---|---|
| **Structure** | Two tabs: **PhotoLibrary** and **Customize**. Customize = left pane (Advanced History, Presets, Projects — PL9 gives the Project palette direct access here), centre viewer, right pane of **palettes**, each a titled fold of **sub-palettes** (tools) with a blue **switch** when active and a per-tool reset. Bottom filmstrip. | [search: https://userguides.dxo.com/photolab/en/interface/ (summary); https://www.edaldridge.com/blog/mastering-the-dxo-photolab-9-interface-effortless-navigation-for-faster-edits] | `PanelLayout.swift`, `DevelopColumn.swift`, `ControlPalette.swift`, `FilmstripView.swift` exist. |
| **Palette model** | Palettes float or dock left/right; drag to reorder; two stock workspaces — **Essential tools** and **All tools**; user **custom workspaces** and **custom palettes** (drag any tool in). **Tool search field** at the top of the right pane; **Favorites** star filter (show only starred tools); **Active-only** filter (show only tools currently applied). Magic-wand "auto" buttons on tools with module-driven defaults. | [search: https://www.tutkit.com/en/text-tutorials/8618-working-area-in-dxo-photolab-3-simply-adjust ; https://forum.dxo.com/t/photolab-8-custom-user-palette/39387] | `Workspace.swift` + `WorkspaceModification.swift` (custom workspaces), `ControlIndex.swift` (tool search?) — verify a Favorites filter and an active-only filter exist. |
| **What dates it** | Palette sprawl (dozens of sub-palettes, many gated by FilmPack/ViewPoint licences that show as locked); a Windows-first skin; no subfolder browsing; the on-canvas equalizer's loss (PL7) still mourned; forum requests for true custom palettes on PL8 show the customisation had limits. **What modernises it**: PL8 Loupe, PL9 AI masking that is fast and local, PL9 dark refresh, Favorites, tool search. | [search: PhotographyLife PL9 review https://photographylife.com/reviews/dxo-photolab-9 ; docs/03 §2.5] | Lumen's Law 21 (disclosure, never modes) is the counter-model to palette sprawl. |
| **Keys** | Standard undo/redo, rating 0–5 keys, colour label keys, before/after toggle, zoom 1:1 / fit, full screen; no Speed-Edit-style hold-key-and-scroll. | [knowledge] | `Keymap.swift`, `KeyGrammar.swift`, `SpeedEdit.swift` present. |

## H · Viewer & scopes

| Capability | How it works | Source | Lumen would need |
|---|---|---|---|
| Histogram | RGB/L histogram with highlight/shadow **clipping toggles** on the toolbar (also as canvas overlays). | [knowledge] | `HistogramView.swift`, `ScopesView.swift`, `RawTruthPanel.swift` (raw-clipping truth — beyond DxO). |
| Loupe Preview | See §A/§E — the only way to see DeepPRIME before export; ≤1600%. | [search] | `LoupeView.swift`. |
| Compare | Before/after (toggle and side-by-side), reference-image compare, virtual copies side by side. | [knowledge] | `CompareView.swift`. |
| Soft proof | Output-profile proof + **Preserve color details** slider + monitor-gamut warning. | [search: Chris Wright wide gamut] | `SoftProofTransform.swift`; check for a gamut-warning overlay. |

## I · Pipeline & performance

| Capability | How it works | Source | Lumen would need |
|---|---|---|---|
| **Raw pipeline** | Optics Module (distortion, vignetting, CA, blur) → DeepPRIME-class joint demosaic+denoise → Wide Gamut working space → tone/colour → output. Refuses raws from bodies with no DxO support (drones, phones, niche); **PL9 added Apple ProRAW + HEIC**. | [docs/03 §2.2] [search: https://support.dxo.com/hc/en-us/articles/6941384594333 ; PetaPixel PL9] | `AppleRawSource.swift` + LibRaw path (docs/14); Lumen never refuses a file. |
| **Compute** | Heavy AI runs at export (batch queue with progress); Loupe/preview compute is windowed. Apple silicon uses GPU/Neural Engine (macOS 12.3 X-Trans slowdown ticket implies Core ML dispatch). Throughput numbers in §E. **9.6 High-Fidelity DNG compression** (≤4× smaller) for the "export DNG then edit elsewhere" workflow. | [search: https://support.dxo.com/hc/en-us/articles/4709167820061 ; Life after Photoshop 9.6] | `RenderCoordinator.swift`, `DraftLadder.swift`, `PlanTableCache.swift` — Lumen's cache-not-files thesis. |
| **Optics Modules** | Per (body, lens) profile built in DxO's lab from target shots; models **vignetting, distortion (keeps max FOV, no forced crop), lateral + longitudinal CA (uses focal length + focus distance), and perceptual blur** per aperture/focal/distance. Assigned from EXIF body+lens IDs; ambiguous matches prompt the user; **downloaded on demand** (free) into a local module store; **>89,000 combos** claimed (docs/03's "~100k+" was an over-round). | [search: https://www.dxo.com/technology/how-dxo-corrects-lens-flaws/ ; https://support.dxo.com/hc/en-us/articles/6940460222109 ; https://www.dxo.com/en/dxo-module/] | `LensCorrections.profile` = Apple/manufacturer opcodes (`EffectsPanel.swift:424`), no blur field, no on-demand module store. |

## J · Library / culling / export / ingest

| Capability | How it works | Source | Lumen would need |
|---|---|---|---|
| **PhotoLibrary** | Folder browser + index (no subfolder recursion); **Projects** (virtual albums); Source Browser search by date, star rating, shooting parameters, keywords; **keywords** hierarchical with drag/drop tree; IPTC edit; **rating 0–5**, **colour labels**, **pick/reject**; PL9: **image stacks** (manual, or auto-group by capture interval, reorder, choose representative) and **Favorites** for folders/projects. Reviewer consensus unchanged: best IQ, weakest DAM; no faces, no map, sluggish browsing. | [docs/03 §2.6] [search: https://lenscraft.co.uk/photo-editing-tutorials/how-to-organise-photos-dxo-photolab/ ; https://www.diyphotography.net/dxo-photolab-9-underrated-features/ ; PhotographyLife PL9] | Schema has `rating`, `flag`, `label`, `stack`/`stack_member`, `keyword` tree (`Schema.swift:44–119`), `FilterBar.swift`, `GridView.swift`. Auto-stack by interval: check `CatalogService.swift`. |
| **Export to disk** | Formats **JPEG** (quality slider; 80–90 advised), **TIFF** 8/16-bit, **DNG** (uncompressed / **High-Fidelity compressed** since 9.6; "all corrections" vs "denoise + optical only" variants); resize by long edge / dimensions with **PPI**; interpolation **Bicubic** (default) / **Bicubic sharper** (web) / Bilinear; **ICC**: As shot / sRGB / Adobe RGB / DxO Wide Gamut / custom (embedding bugs reported); **watermark** (text or image, position/opacity — not on "export without corrections"); **several export presets run simultaneously** distinguished by filename suffix; also Export to Lightroom / to application / to Nik. **No output-sharpening control** — users lean on Bicubic sharper or Nik Sharpener. | [search: https://userguides.dxo.com/photolab/en/exporting-images/ (summary); https://forum.dxo.com/t/discovery-ability-to-control-degree-of-output-sharpening-applied-by-bicubic-resizing-algorithm/54931 ; https://forum.dxo.com/t/output-sharpenning-in-photolab-directly/13496] | `ExportFormat jpeg/heif/tiff/png` (no DNG), `ExportColorSpace` ×5, `ResizeMode` ×6, `OutputSharpen medium×amount`, `Watermark` text-only with position/opacity, `MetadataPolicy` (`ExportRecipe.swift:23–236`). Lumen leads on output sharpening; **no image watermark, no DNG out, no multi-preset-simultaneous export** (check `ExportSheet.swift`). |
| **Ingest** | No dedicated import/rename/backup; files are edited in place, sidecars written alongside. | [knowledge] | `IngestSheet.swift`, `RenameTemplate.swift` — beyond DxO. |

## K · Crop / lens / geometry

| Capability | How it works | Source | Lumen would need |
|---|---|---|---|
| Crop & Horizon | Crop with aspect presets and auto (module-based) mode; Horizon slider with on-canvas line tool. | [knowledge] | `CropGeometry.swift`, `Straighten.swift`, `CropPanel.swift`. |
| Distortion / Vignetting / CA | Module-driven auto with manual intensity; distortion correction keeps max FOV; **Lens softness** = Lens Sharpness (§E). Manual distortion (barrel/pincushion/fisheye) when no module. | [search: how-dxo-corrects-lens-flaws] | `LensCorrections` (3 fields) — no manual distortion slider (grep `distortion` = 2 files). |
| Perspective | **ViewPoint** (separate paid product; integrated palette when licensed): keystone (4- and 8-point), volume-deformation for wide-angle faces, ReShape warp, miniature effect. | [knowledge] | Absent; docs/09 roadmap item. |

## L · State / undo

| Capability | How it works | Source | Lumen would need |
|---|---|---|---|
| **Advanced History** | Left-pane palette in Customize: every step with **tool name and value**, chronological from "image opened" and "default corrections applied"; click any row to revert to that state (later rows greyed until a new edit truncates). Persists per image across sessions (in the database and `.dop`). | [search: https://userguides.dxo.com/photolab/en/the-customize-tab/ (summary)] | `HistoryStack.record/undo/redo/snapshot` (`HistoryStack.swift:114–208`), `history` table with `label` (`Schema.swift:68–72`), `HistoryCoalescing.swift`. Named snapshots present; verify the values-in-rows legibility (Law 24). |
| Virtual copies | Any number per master; compared side by side. | [knowledge] | Check `CatalogStore.swift` for revisions vs copies. |
| Undo/redo | Standard, unlimited within session. | [knowledge] | Present. |

## M · Recipe / serialization / sidecars

| Capability | How it works | Source | Lumen would need |
|---|---|---|---|
| **.dop sidecar** | One `<image>.<ext>.dop` per image (text, key-value/structured), written automatically beside the file; carries all corrections, virtual copies, and history; database is the index. Preferences toggle for auto-import/export of sidecars. Version-migrated on open (PL major versions add keys; older PL cannot read newer). | [knowledge] | `XMPSidecar.swift`, `XMPMerge.swift`, `Recipe` JSON with `pipelineVersion` and `CanonicalJSON`/`Fingerprint` (`Recipe.swift:26–34`). Lumen uses XMP + own JSON — interop target is LR, not DxO. |
| Presets | `.preset` files (XML-like) exportable/importable; partial presets. | [knowledge] | `LookSubset.swift`. |
| Local-edit presets with AI re-targeting (PL9) | A preset can carry an AI-mask Subject Type so it re-detects on each image it is applied to. | [search: DxO features summary] | Recipe masks carry `MaskKind.ai*` components (`RecipeMasks.swift:250–255`); verify that applying a look re-runs the matte per image. |

---

## New relative to docs/02–03

- **PhotoLab 10 shipped today (2026-09-01)**: Depth Masks (AI depth map from any raw; foreground/mid/background bands), Control Brush (brush × U Point similarity), **elliptical Control Points**, Nik-9 tone-zone grading wheel; $249.99 / $129.99 upgrade. "AI add-ons" appear in PetaPixel's headline — detail unverified.
- **PhotoLab 9 (Sep 2025) closed docs/03's "no AI masking" gap**: hover-click / box / Subject-Type AI masks (sky, subject, background, people, animals, flowers, vehicles, hair, faces, clothes), local DeepPRIME + demosaic + Lens Sharpness inside masks, ProRAW/HEIC, stacks, Favorites. 9.2 matting; **9.6 (Mar 2026) XD3 for Bayer + AI-mask diffusion/feather + High-Fidelity DNG compression**.
- **DeepPRIME 3** tier exists (≈3× faster than XD2s, ≈4 s/image on M1 Max) alongside XD3; PureRAW 6 and Nik Collection 9 exist.
- Exact slider defaults: PRIME/HQ Luminance 40 / Chrominance 100 / Low frequency 75 / Dead pixels 24 / Maze 30; DeepPRIME = Luminance + Dead pixels; XD2s/XD3 add **Force details**; X-Trans loses Dead pixels and Maze. Lens Sharpness Global default **+1.00 since PL 6.3**, Details 50, Bokeh 50. USM start 100/0.5/4.
- Measured Apple-silicon throughput: XD2s **3.4 MP/s** on M1 Max Studio.
- PL8's **Loupe Preview** (≤1600%) is the DeepPRIME preview surface; PL9 adds an optional full-screen DeepPRIME preview on strong hardware.
- Color Rendering: **ICC input profiles dropped in PL7**; DCP with DxO-vs-Adobe curve choice; Category/Rendering/Intensity/Protect-saturated structure.
- Optics Modules: **>89,000 combos** (not "~100k+"); four modelled defects incl. **longitudinal CA via focus distance**; distortion keeps max FOV.
- Export: **no output-sharpening control** at all (Bicubic sharper is the workaround); simultaneous multi-preset export via suffix; DNG "denoise+optical only" variant; ICC embedding bugs.
- **FilmPack 8 (Sep 2025)**: 153 renderings, 4 digital-camera renderings, **unique grain pattern per rendering**, grain matrices derived from shadow/mid/highlight scans, Time Warp's Time Travel slider, light-leak Defect/Intensity(50) model, luminosity masks inside FilmPack, 10+ scan-inversion curve presets. **No halation control** found — Lumen's physical halation stage is ahead.
- FilmPack-gated **Fine contrast** with Highlights/Midtones/Shadows sub-sliders (tone-range-limited micro-contrast) — absent in Lumen.
- Spot-Weighted Smart Lighting: detected-face count shown in the sub-palette; user-drawn "preserved areas"; defaults to Slight.

## Could not verify

- Lens Sharpness Global range (−3…+3 is inherited from docs/03; forum posts only confirm default +1.00). Uniformity slider numeric range. Selective Tone ranges (−100…+100 from training).
- DeepPRIME XD3 Bayer timings on M-series; whether PL10 changed the preview model (live DeepPRIME in the main viewer) or which features are the "AI add-ons" and whether they are paid.
- FilmPack 8 per-category stock counts (negative / slide / B&W / instant), the second Time Warp slider's name, any Size-vs-print-size semantics in the grain palette, and DxO Wide Gamut primaries.
- Whether Luminosity masks arrived in PL7 or PL6.x (docs/17 item 12 stays open); PL10 Depth Mask model size/download behaviour.
- PureRAW 6 and Nik Collection 9 feature lists; PhotoLab's exact keyboard map; `.dop` file grammar details.
- dxo.com and userguides.dxo.com are egress-blocked, so DxO-authored specifics are from search summaries, not the pages.
