# W2 — F4 · Mask panel (post-rebuild)

Read `briefs/w2-common.md` first. Output: `docs/audit-2026-09/w2/F4.md`.

## The question
A line-by-line Lightroom parity checklist of the panel as of 64ae6da against R1's Masks-panel section: every control LR has, present here or not; every defect visible in the code (widths at 272 pt, insets, pitch, dead state, controls wired to nothing — `brushParameters` was declared and uncalled until cc82116; find the next one). K-001 nine writers of `soloMaskOverlay`, K-003 Edge as a transfer graph, K-006 the register bundle, K-012 density.

## Files you own (read every line)
- `Sources/LumenApp/MaskPanel.swift`
- `Sources/LumenApp/MaskFloatingPanel.swift`
- `Sources/LumenApp/AppState.swift (§mask overlay, maskPanel*, maskCreateBoardOpen only)`
- `Sources/LumenApp/LumenBehaviourGlyph.swift`
- `Sources/LumenCore/Recipe/RecipeMasks.swift (LocalAdjust.Group only)`

## Spec and prior audit to read
- `docs/37-the-panel-audit.md`
- `docs/36-masking-round-three.md §6`
- `w1/r1-lightroom.md §F`
- `docs/audit-2026-09/w1/gap-table.md` §F

## Known-open rows to re-verify (STILL-OPEN / FIXED-SINCE / CHANGED / REJECTED, with file:line)
- **K-001** (S2, docs/37 §5.1): Overlay precedence still expressed by write-order across nine writers of `soloMaskOverlay`; only three guards live in `MaskOverlayRule`
- **K-002** (S3, docs/37 §5.2): Navigator split blocked on `refreshMaskThumbnails` rebuilding serially, one `await` per mask
- **K-003** (S2, docs/37 §5.3): Edge controls are a Levels dialog on alpha; should be one transfer graph; Follow → Off/Gentle/Strong; Expand+Soften → one overlay drag
- **K-006** (S2, docs/37 §5.6): Control-register bundle: toggle rows identical to a slider at zero; 9 label-left rows with no value column; 22/28/33 pt pitch; 6 radii vs 3 tokens; Light & Colour lack modified dot + Reset; mask Temp on linear Kelvin axis; mask Exposure/Contrast missing coloured tracks; `autoName` wired to unreachable placeholder
- **K-009** (—, docs/36 open): Per-component refinement: do NOT build as specified (12 sliders on 3 components); open design question
- **K-012** (S3, docs/36 open): Density toggle: filter landed, compact row height did not
- **K-072** (—, docs/32 §4): Masks polish round two — **deferral lifted**
- **K-077** (S3, docs/27 §3): Masked adjustment proofs deferred — runner needs mask-raster support

## Cross-cutting rows (FYI — disposition only if you touch them)
- **K-014**: `check-swift-surface.py` cannot see protocol conformance
- **K-058**: Texture GPU gain has no ±4 EV limit; gamut flag on opposite sides of grain in the two renderers; album counts offline frames; `rebuild` writes bare LFs into CRLF sidecar; a Lumen element name stripped inside foreign provenance
- **K-073**: Everything else stays ranked in docs/31
- **K-102**: LumenAppTests exists (9 files) but no CI lane runs it under a filter

## Timebox
30 minutes. Read-only. No git.
