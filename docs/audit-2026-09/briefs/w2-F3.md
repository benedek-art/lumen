# W2 — F3 · Mask persistence & data integrity

Read `briefs/w2-common.md` first. Output: `docs/audit-2026-09/w2/F3.md`.

## The question
The data-loss set: strokes past ~26,300 points deleted by the sidecar; painting with a multi-photo selection overwriting other photos' strokes; inverted mask with a missing input selecting the whole frame; K-018 strokes outside both backups; K-015 same-basename sidecar collision. Round-trip fidelity of every MaskComponent field through XMP and the catalog.

## Files you own (read every line)
- `Sources/LumenCore/XMP/XMPSidecar.swift`
- `Sources/LumenCore/XMP/XMPMerge.swift`
- `Sources/LumenCore/Model/BrushStroke.swift (encoding only)`
- `Sources/LumenCore/Catalog/BlobStore.swift`
- `Sources/LumenCore/Recipe/RecipeMasks.swift (Codable and validation only)`
- `Sources/LumenApp/AppState.swift (strokeSets, paste settings with masks, multi-photo edits only)`
- `Sources/LumenApp/CatalogService.swift (sidecar and blob paths only)`

## Spec and prior audit to read
- `docs/15-catalog.md`
- `docs/08-spec-masking.md`
- `docs/31-defect-audit.md data-loss`
- `docs/audit-2026-09/w1/gap-table.md` §F

## Known-open rows to re-verify (STILL-OPEN / FIXED-SINCE / CHANGED / REJECTED, with file:line)
- **K-011** (S3, docs/36 open): Alpha PNG export/import of a mask — not built
- **K-018** (**S1**, docs/31 r1 data-loss): Brush strokes in neither backup (blob store outside `VACUUM INTO` and outside the sidecar) — restored catalog gives masks that rasterize empty
- **K-072** (—, docs/32 §4): Masks polish round two — **deferral lifted**
- **K-077** (S3, docs/27 §3): Masked adjustment proofs deferred — runner needs mask-raster support

## Cross-cutting rows (FYI — disposition only if you touch them)
- **K-014**: `check-swift-surface.py` cannot see protocol conformance
- **K-058**: Texture GPU gain has no ±4 EV limit; gamut flag on opposite sides of grain in the two renderers; album counts offline frames; `rebuild` writes bare LFs into CRLF sidecar; a Lumen element name stripped inside foreign provenance
- **K-073**: Everything else stays ranked in docs/31
- **K-102**: LumenAppTests exists (9 files) but no CI lane runs it under a filter

## Timebox
30 minutes. Read-only. No git.
