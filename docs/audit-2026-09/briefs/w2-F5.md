# W2 — F5 · AI & range masks

Read `briefs/w2-common.md` first. Output: `docs/audit-2026-09/w2/F5.md`.

## The question
Select Subject/Sky/Background quality and speed vs LR/C1/Luminar; K-084 unoffered kinds — confirm no path reaches them; K-085 matte orientation never verified (what test could?); `lumaRange` vs `luminosity` duplication; colour/luma range UX (eyedropper, range/feather handles) vs the gap table; per-person parts.

## Files you own (read every line)
- `Sources/LumenPipeline/VisionMattes.swift`
- `Sources/LumenCore/Image/MaskRaster.swift (lumaRange, luminosity, colorRange, similarity, similarityLine, aiSubject/Sky/Background/Object/Person/Landscape, depthRange only)`
- `Sources/LumenCore/Recipe/RecipeMasks.swift (MaskKind, LuminositySeries, MaskChannel only)`
- `Sources/LumenApp/MaskPanel.swift (range and AI component editors, kind rosters only)`
- `Sources/LumenApp/RenderCoordinator.swift (mattes only)`
- `Sources/LumenApp/AppState.swift (availableMattes, pendingMattes only)`

## Spec and prior audit to read
- `docs/08-spec-masking.md §AI`
- `docs/36-masking-round-three.md #8`
- `BUILDING.md §AI masks`
- `docs/audit-2026-09/w1/gap-table.md` §F

## Known-open rows to re-verify (STILL-OPEN / FIXED-SINCE / CHANGED / REJECTED, with file:line)
- **K-013** (—, docs/36 open): Six model-dependent mask kinds need licence review, model conversion, download UX, app-size decision
- **K-072** (—, docs/32 §4): Masks polish round two — **deferral lifted**
- **K-077** (S3, docs/27 §3): Masked adjustment proofs deferred — runner needs mask-raster support
- **K-084** (S2, BUILDING): Sky, Object, Landscape, Depth Range have no model and rasterize to nothing (now unoffered in picker; verify no path reaches them)
- **K-085** (S2, BUILDING): Vision path never run on hardware — matte orientation argued from convention

## Cross-cutting rows (FYI — disposition only if you touch them)
- **K-014**: `check-swift-surface.py` cannot see protocol conformance
- **K-058**: Texture GPU gain has no ±4 EV limit; gamut flag on opposite sides of grain in the two renderers; album counts offline frames; `rebuild` writes bare LFs into CRLF sidecar; a Lumen element name stripped inside foreign provenance
- **K-073**: Everything else stays ranked in docs/31
- **K-102**: LumenAppTests exists (9 files) but no CI lane runs it under a filter

## Timebox
30 minutes. Read-only. No git.
