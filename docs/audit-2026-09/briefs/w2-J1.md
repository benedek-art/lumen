# W2 — J1 · Catalog store

Read `briefs/w2-common.md` first. Output: `docs/audit-2026-09/w2/J1.md`.

## The question
Data safety: transactions, WAL, crash mid-write, K-017 catalog loss notice, K-019 backup never pruned, K-052 FTS never rebuilt, K-053/K-054 label and rating loss, K-055 no LIMIT per keystroke, K-056 first-open cost, K-057 prefix vs infix, K-059 pivot robustness; scan reconciliation correctness on rename/move.

## Files you own (read every line)
- `Sources/LumenCore/Catalog/CatalogStore.swift`
- `Sources/LumenCore/Catalog/SQLite.swift`
- `Sources/LumenCore/Catalog/Schema.swift`
- `Sources/LumenCore/Catalog/PhotoMetadata.swift`
- `Sources/LumenCore/Catalog/QuickSignature.swift`
- `Sources/LumenApp/CatalogService.swift (threading and sidecar discipline)`
- `Sources/LumenApp/LumenApp.swift (close/backup wiring only)`

## Spec and prior audit to read
- `docs/15-catalog.md`
- `docs/31-defect-audit.md round two`
- `docs/audit/library-ux.md`
- `docs/audit-2026-09/w1/gap-table.md` §J

## Known-open rows to re-verify (STILL-OPEN / FIXED-SINCE / CHANGED / REJECTED, with file:line)
- **K-015** (**S1**, docs/31 r1 data-loss): Two RAWs with the same base name share one `NAME.xmp`; NEF+DNG editing destroys the NEF recipe and `sidecarWins` writes it into the catalog
- **K-016** (**S1**, docs/31 r1 data-loss): Export presets decoded with `try?` against a strict codec — one new field reverts the whole preset list to the stock four
- **K-017** (S2, docs/31 r1 data-loss): Losing the catalog loses the session; only notice is 10 pt text in a hideable sidebar
- **K-018** (**S1**, docs/31 r1 data-loss): Brush strokes in neither backup (blob store outside `VACUUM INTO` and outside the sidecar) — restored catalog gives masks that rasterize empty
- **K-019** (S2, docs/31 r1 data-loss): `recoverIfNeeded` has nothing to recover from; `backup()`'s only caller is a menu item; nothing prunes
- **K-052** (**S1**, docs/31 r2): Text filter returns nothing permanently once `cache.db` is recreated — `photo_fts` lives in the disposable DB, nothing rebuilds it
- **K-053** (**S1**, docs/31 r2): A colour label Lumen cannot name is deleted from the sidecar on the first culling keystroke
- **K-054** (**S1**, docs/31 r2): `xmp:Rating="-1"` (LR reject) silently rewritten as 0
- **K-055** (S2, docs/31 r2): Grid query materializes every row with no LIMIT per keystroke — 350 ms for 50k rows on the thumbnail queue, no debounce
- **K-056** (S2, docs/31 r2): First open of a card ≈1 s at 5k frames, ≈10 s at 50k, inside one `queue.sync`
- **K-057** (S3, docs/31 r2): Text chip is prefix-only on FTS and infix on the fallback

## Cross-cutting rows (FYI — disposition only if you touch them)
- **K-014**: `check-swift-surface.py` cannot see protocol conformance
- **K-058**: Texture GPU gain has no ±4 EV limit; gamut flag on opposite sides of grain in the two renderers; album counts offline frames; `rebuild` writes bare LFs into CRLF sidecar; a Lumen element name stripped inside foreign provenance
- **K-073**: Everything else stays ranked in docs/31
- **K-102**: LumenAppTests exists (9 files) but no CI lane runs it under a filter

## Timebox
30 minutes. Read-only. No git.
