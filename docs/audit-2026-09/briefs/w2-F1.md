# W2 — F1 · Mask canvas

Read `briefs/w2-common.md` first. Output: `docs/audit-2026-09/w2/F1.md`.

## The question
"Do everything in the circle." Hold every on-canvas gesture up against LR's grammar as R1 states it (move from inside, resize on edge handles, rotate on the edge, feather ring, ⌥/⇧/⌘ modifiers, duplicate, invert, pin behaviour): present / partial / absent, with the code path. K-004 brush pin, K-022 rotation units (re-verify after cc82116), polygon self-intersection, brush cursor/pressure/stabiliser, foreign pin swallowing pan.

## Files you own (read every line)
- `Sources/LumenApp/MaskCanvas.swift`
- `Sources/LumenCore/Interaction/MaskHandles.swift`
- `Sources/LumenCore/Interaction/BrushStabilizer.swift`
- `Sources/LumenCore/Interaction/MaskOverlayRule.swift`
- `Sources/LumenApp/ViewerOverlays.swift (MaskOverlayView only)`
- `Sources/LumenCore/Model/BrushStroke.swift (geometry only)`

## Spec and prior audit to read
- `docs/08-spec-masking.md`
- `docs/37-the-panel-audit.md §5.4`
- `w1/r1-lightroom.md §F`
- `docs/audit-2026-09/w1/gap-table.md` §F

## Known-open rows to re-verify (STILL-OPEN / FIXED-SINCE / CHANGED / REJECTED, with file:line)
- **K-004** (S2, docs/37 §5.4): Brush masks have no pin — `MaskCanvas.anchor(of:)` hits `default: continue` for `.brush`; Strength scrub on pin never built
- **K-022** (S2, docs/31 r1 correctness): Radial ellipse rotates in per-axis normalized units while `MaskRaster` rotates in long-edge units — handle 1.5× off in y on 3:2 at 45° (partly addressed by `radialOffset` this session; re-verify)
- **K-072** (—, docs/32 §4): Masks polish round two — **deferral lifted**
- **K-077** (S3, docs/27 §3): Masked adjustment proofs deferred — runner needs mask-raster support

## Cross-cutting rows (FYI — disposition only if you touch them)
- **K-014**: `check-swift-surface.py` cannot see protocol conformance
- **K-058**: Texture GPU gain has no ±4 EV limit; gamut flag on opposite sides of grain in the two renderers; album counts offline frames; `rebuild` writes bare LFs into CRLF sidecar; a Lumen element name stripped inside foreign provenance
- **K-073**: Everything else stays ranked in docs/31
- **K-102**: LumenAppTests exists (9 files) but no CI lane runs it under a filter

## Timebox
30 minutes. Read-only. No git.
