# W2 — E2 · Sharpening

Read `briefs/w2-common.md` first. Output: `docs/audit-2026-09/w2/E2.md`.

## The question
K-075/K-088 capture radius stored and never applied, Richardson–Lucy uncalled and untested — wire or remove, with RT's capture sharpening (R4 has the source) as the reference; K-082/K-097 radius not in output pixels so export is less sharp than the judged frame; masking and halo suppression vs LR; output sharpening per medium.

## Files you own (read every line)
- `Sources/LumenCore/Image/DetailEngine.swift (S4 capture sharpen and S12 sharpen only)`
- `Sources/LumenCore/Image/SpatialOps.swift (Richardson–Lucy only)`
- `Sources/LumenCore/Recipe/Recipe.swift (CaptureSharpen, ManualSharpen only)`
- `Sources/LumenCore/Export/ExportRecipe.swift (OutputSharpen only)`
- `Sources/LumenApp/DetailPanel.swift (Capture Sharpening and Sharpening sections only)`
- `Sources/LumenPipeline/Kernels.swift (sharpenDelta, edgeMap only)`

## Spec and prior audit to read
- `docs/06-spec-detail.md`
- `docs/27-slider-verification.md §4`
- `w1/r4-opensource.md §E`
- `docs/audit-2026-09/w1/gap-table.md` §E

## Known-open rows to re-verify (STILL-OPEN / FIXED-SINCE / CHANGED / REJECTED, with file:line)
- **K-075** (S2, docs/27 §3): `detail.capture.radius` stored and not applied — Richardson–Lucy has no caller; wire-or-remove
- **K-076** (S3, docs/27 §3): `detail.capture.amount` and `denoise.amount` (AI) measurable only on macOS lane
- **K-082** (S2, docs/27 §4): Sharpen radius in OUTPUT pixels; preview/export disagreement recorded
- **K-088** (S2, BUILDING): Capture sharpening (S4) unchanged; `richardsonLucy` uncalled and untested
- **K-097** (S2, BUILDING): Creative sharpening not resolution-scaled — export less sharpened than judged; no surface shows it

## Cross-cutting rows (FYI — disposition only if you touch them)
- **K-014**: `check-swift-surface.py` cannot see protocol conformance
- **K-058**: Texture GPU gain has no ±4 EV limit; gamut flag on opposite sides of grain in the two renderers; album counts offline frames; `rebuild` writes bare LFs into CRLF sidecar; a Lumen element name stripped inside foreign provenance
- **K-073**: Everything else stays ranked in docs/31
- **K-102**: LumenAppTests exists (9 files) but no CI lane runs it under a filter

## Timebox
30 minutes. Read-only. No git.
